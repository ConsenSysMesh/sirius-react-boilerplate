// import {  } from '../actions/test'
import { initStateTest } from './initialState'

export default function(state = initStateTest, action) {
  switch (action.type) {
    default:
      return state
  }
}
